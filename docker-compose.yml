---
version: '3'
services:
  jenkins:
    container_name: jenkins
    build:
        context: jenkins-ansible
    image: jenkins-ansible
    ports:
      - "8081:8080"
    volumes:
      - "$PWD/jenkins_home:/var/jenkins_home"